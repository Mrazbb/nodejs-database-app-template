# /bin/bash


# Push to git

export SSH_AUTH_SOCK=$(ls -t /tmp/ssh-**/* | head -1)
export SSH_AUTH_SOCK=$(ls -t /tmp/ssh-**/* | head -1)
export SSH_AUTH_SOCK=$(ls -t /tmp/ssh-**/* | head -1)
export SSH_AUTH_SOCK=$(ls -t /tmp/ssh-**/* | head -1)
export SSH_AUTH_SOCK=$(ls -t /tmp/ssh-**/* | head -1)
export SSH_AUTH_SOCK=$(ls -t /tmp/ssh-**/* | head -1)
git push 