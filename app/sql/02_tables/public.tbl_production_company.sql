CREATE TABLE "public"."tbl_production_company" (

    -- IDENTIFIER
    "id" INTEGER NOT NULL,

    -- FIELDS
    "company_id" INTEGER, 
    "name" TEXT,

    -- CONSTRAINTS
    PRIMARY KEY ("id")
); 